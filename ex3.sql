CREATE DATABASE IF NOT EXISTS `frameworks` CHARACTER SET 'utf8';
-- CREATE DATABASE pour créer une base de donnée
-- IF NOT EXISTS, si elle n'existe pas, à faire si plein de commandes sinon ça s'arrete là
-- CHARACTER SET encodage utf8 pour garder le meme encodage que le site
